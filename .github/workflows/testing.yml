name: test workflow

on:
  push:

jobs:
  setup-go:
    runs-on: ubuntu-latest
    steps:
      - name: first setup
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: echo version
        run: go version
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: echo version
        run: go version
