name: test workflow

on:
  push:

jobs:
  setup-go:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: checkout again
        uses: actions/checkout@v4
      - name: first setup
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: echo version
        run: go version
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: echo version
        run: go version
